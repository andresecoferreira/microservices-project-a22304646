name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: read

jobs:
  product:
    uses: ./.github/workflows/reusable-image.yml
    with:
      service: product-service
    secrets: inherit
